import Logo from './Logo';
import NavBar from './NavBar';

const Header = () => {
  return (
    <header className="header-app">
      <Logo />
      <NavBar />
    </header>
  );
};
export default Header;
